body {
  font-family: Arial, sans-serif;
  margin: 0;
  line-height: 1.5;
}

.container {
  max-width: 900px;
  margin: 0 auto;
  padding: 24px;
}

header {
  background: #111;
  color: white;
}

button {
  padding: 10px 14px;
  border: none;
  border-radius: 10px;
  cursor: pointer;
}
